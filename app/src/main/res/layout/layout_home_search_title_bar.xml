<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="44dp">

    <TextView
            android:id="@+id/txtView_searchTitle"
            android:layout_marginStart="@dimen/large_margin_size"
            android:layout_marginEnd="@dimen/large_margin_size"
            android:layout_width="match_parent"
            android:layout_marginTop="4dp"
            android:hint="@string/search"
            android:textSize="@dimen/txtSize_14"
            android:gravity="center_vertical"
            android:paddingStart="@dimen/normal_margin_size"
            android:paddingEnd="@dimen/normal_margin_size"
            android:layout_marginBottom="4dp"
            android:background="@drawable/shape_round30_black_divider"
            android:layout_height="match_parent"/>

</RelativeLayout>